# 录音技术验证（2026-08-17）

## 结论

- 真实双音源 probe：**PASS**
- 判定标准：系统音频和麦克风样本数都大于 0；两路 PTS 均有效、为有限数值且严格递增；恰好收到 1 个无错误的 stopped 事件；未注册或保存屏幕视频帧。
- 这次 probe 没有注册 `.screen` output，也没有创建任何视频文件。

## 运行环境

- 开始：2026-08-17T10:06:45Z
- 结束：2026-08-17T10:07:17Z
- 请求采集时长：30 秒
- 系统：Version 26.5.1 (Build 25F80)
- 命令：`env DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer /usr/bin/xcrun swift run RecorderProbe dual-capture --seconds 30 --report docs/verification/2026-08-17-录音技术验证.md`
- 运行前屏幕与系统音频录制权限：true
- 运行后屏幕与系统音频录制权限：true
- 运行前麦克风授权状态：authorized
- 运行后麦克风授权状态：authorized

## 原始关键结果

- 系统音频样本数：1567
- 麦克风样本数：2916
- 系统音频无效 PTS 数：0
- 麦克风无效 PTS 数：0
- 系统音频 PTS 严格递增：true
- 麦克风 PTS 严格递增：true
- 视频样本数：0（未注册 `.screen` output）
- 默认麦克风变更事件：none
- warning：none
- stopped 事件数：1
- 启动失败：none
- 采集中止失败：none

## 未验证项

- 样本数只证明 ScreenCaptureKit 实际交付了两路音频 buffer；本 probe 不写文件，也不判断内容是否为静音。
- 本次运行没有主动切换默认输入设备，因此只有实际发生切换时才能验证热更新事件。
- 如果状态为 `NOT PASSED`，不能据此声称真实双音源采集已经通过；需要按上面的权限或输入条件处理后重新运行。

## Task 5：强制结束与媒体恢复验证

### 结论

- 强制结束后恢复：**PASS**
- 唯一运行时策略：`FragmentedMOVWriter`。
- MOV 使用 1 秒 initial fragment 和 10 秒后续 fragment；正常结束或下次启动恢复时，输出 AAC M4A。
- `SegmentedM4AWriter` 已实现 10 秒分段和 `AVMutableComposition` 合并，但由于首选策略已通过，factory 不会选用它。

### 强制结束操作

- working：`/tmp/meeting-recorder-task5-recovery-probe-2026-08-17.mov`
- recovered：`/tmp/会议录音-2026-08-17-task5-未完整恢复.m4a`
- log：`/tmp/meeting-recorder-task5-probe-2026-08-17.log`
- 启动前三个路径均已确认不存在，没有删除或覆盖旧的 `/tmp` 文件。
- 直接启动已构建的 `RecorderProbe`，记录精确 PID `41816`。
- probe 持续生成 48 kHz stereo Float32、440 Hz、幅度 0.25 的正弦波；不是静音。
- 只向 PID `41816` 发送 TERM，没有使用 `pkill`。等待结果为 `143`，随后 `kill -0 41816` 确认该进程已停止。

强制结束时的关键原始输出：

```text
STARTED exactPID=41816
ready pid=41816 signal=TERM
STOPPED exactPID=41816 waitStatus=143
-rw-r--r--@ 1 alan wheel 209544 Aug 17 18:42 /tmp/meeting-recorder-task5-recovery-probe-2026-08-17.mov
writtenFrames=480000 seconds=10
writtenFrames=864000 seconds=18
```

### AVFoundation 恢复与解码检查

恢复命令的关键原始输出：

```text
durationSeconds=10.964000
audioTracks=1
aacTrack=true
playable=true
peakAmplitude=0.258452
result=PASS
recoverExit=0
-rw-r--r--@ 1 alan wheel 104389 Aug 17 18:42 /tmp/会议录音-2026-08-17-task5-未完整恢复.m4a
workingRetained=false
```

`duration` 和 `isPlayable` 由 `AVURLAsset` 读取，音轨编码通过 format description 确认为 AAC；`peakAmplitude` 由 `AVAssetReader` 把恢复文件实际解码为 Float32 PCM 后计算。这不是根据 writer 配置推测的结果。

### 未验证项

- 本次使用 TERM 强制结束，没有模拟断电、内核崩溃或磁盘硬件故障。
- 已验证 10.964 秒恢复媒体，未做数小时长录音、磁盘写满或休眠唤醒测试。
- Task 5 单独验证了转换、混音和 writer；ScreenCaptureKit 真实双路样本到 writer 的整体会话编排属于后续任务。
- 因 fragmented MOV 已通过，没有对未选中的 segmented M4A fallback 再做强制结束 probe；其正常分段和合并路径已由单元测试覆盖。

## Task 5 Fix Round 1：两种 writer 的强制结束复测

### 结论

- fragmented MOV：**PASS**，恢复后 10.964 秒，AAC、可播放、非静音。
- segmented manifest：**PASS**，恢复后 20.958667 秒，AAC、可播放、非静音。
- factory 仍然只返回 `FragmentedMOVWriter`。Segmented 是完整可恢复的备选实现，但不参与运行时随机选择。

两次启动前都明确检查了本任务的 working/output/log 路径，结果均为 `ABSENT`。只向 probe 回报的精确 PID 发送 TERM，没有使用 `pkill`。

### Fragmented MOV

```text
STARTED exactPID=49348
ready pid=49348 signal=TERM strategy=fragmented
STOPPED exactPID=49348 waitStatus=143
writtenFrames=960000 seconds=20
durationSeconds=10.964000
audioTracks=1
aacTrack=true
playable=true
peakAmplitude=0.258452
result=PASS
recoverExit=0 strategy=fragmented
workingRetained=false
```

### Segmented manifest

TERM 时 manifest 已包含两个完成的 M4A segment 和一个未完成 MOV segment。

```text
STARTED exactPID=49646
ready pid=49646 signal=TERM strategy=segmented
STOPPED exactPID=49646 waitStatus=143
writtenFrames=1056000 seconds=22
durationSeconds=20.958667
audioTracks=1
aacTrack=true
playable=true
peakAmplitude=0.259033
result=PASS
recoverExit=0 strategy=segmented
```

发布后 manifest、两个已完成 segment 和未完成 segment 全部清理：

```text
cleaned /tmp/meeting-recorder-task5-fix1-segmented-2026-08-17.mov
cleaned /tmp/.meeting-recorder-task5-fix1-segmented-2026-08-17.mov.segment-000000.m4a
cleaned /tmp/.meeting-recorder-task5-fix1-segmented-2026-08-17.mov.segment-000001.m4a
cleaned /tmp/.meeting-recorder-task5-fix1-segmented-2026-08-17.mov.segment-000002.inprogress.mov
```

`duration` 和 `isPlayable` 由 `AVURLAsset` 实际读取，AAC 由音轨 format description 确认，`peakAmplitude` 由 `AVAssetReader` 实际解码 Float32 PCM 计算。

### 未验证项

- 两种策略都验证了 TERM，仍未模拟断电、内核崩溃、磁盘损坏或磁盘写满。
- 未做数小时连续录音、休眠唤醒，或真实 ScreenCaptureKit 双路到 writer 的端到端会话验证。

## Task 5 Fix Round 2：inode commit 与超短 current segment 恢复

### 结论

- Fragmented 正常 TERM：**PASS**，10.964 秒 AAC，可播放，非静音。
- Segmented 在 completed index `0` 完成、current index `1` 刚建立且只有 302 bytes 时 TERM：**PASS**，跳过不可导出 current，仅用 completed segment 恢复并保留 10 秒时间轴。
- Recovery claim 现在直接锁 working inode；publish 前在该 inode 上持久化 pending marker，publish 成功即为 commit。清理 working 失败不会把成功报成失败，marker 会阻止第二 target 重复发布。

两次启动前均对本轮明确命名的 `/tmp` working、output、log 和 segment 做了 `ABSENT` 检查。只向 probe 回报的精确 PID 发送 TERM，等待码均为 `143`，随后用 `kill -0` 确认已停止。

### Fragmented 正常 TERM

```text
STARTED exactPID=55392
ready pid=55392 signal=TERM strategy=fragmented
writtenFrames=576000 seconds=12
STOPPED exactPID=55392 waitStatus=143
CONFIRMED_STOPPED exactPID=55392
durationSeconds=10.964000
audioTracks=1
aacTrack=true
playable=true
peakAmplitude=0.258452
result=PASS
recoverExit=0 strategy=fragmented
workingRetained=false
```

### Segmented 超短 current segment TERM

TERM 前 manifest 内容证明 `completed[0].index == 0`、`current.index == 1`。已完成 M4A 为 91,147 bytes，current MOV 仅 302 bytes：

```text
STARTED exactPID=55967
ready pid=55967 signal=TERM strategy=segmented
writtenFrames=480000 seconds=10
STOPPED exactPID=55967 waitStatus=143
CONFIRMED_STOPPED exactPID=55967
durationSeconds=10.000000
audioTracks=1
aacTrack=true
playable=true
peakAmplitude=0.258452
result=PASS
recoverExit=0 strategy=segmented-short-current
```

恢复后的清理检查：

```text
cleaned /tmp/meeting-recorder-task5-fix2-segmented-short-current-2026-08-17.mov
cleaned /tmp/.meeting-recorder-task5-fix2-segmented-short-current-2026-08-17.mov.segment-000000.m4a
cleaned /tmp/.meeting-recorder-task5-fix2-segmented-short-current-2026-08-17.mov.segment-000001.inprogress.mov
cleaned /tmp/.meeting-recorder-task5-fix2-segmented-short-current-2026-08-17.mov.segment-000001.m4a
```

`duration`、AAC 和 playable 来自 `AVURLAsset` 实际读取，`peakAmplitude` 来自 `AVAssetReader` 实际解码 Float32 PCM。

### 未验证项

- 本轮验证了精确 PID TERM，不等于断电一致性验证。Output/working 父目录尚未执行目录 `fsync`，断电、内核崩溃、磁盘损坏和磁盘写满仍未验证。
- 未做数小时连续录音、休眠唤醒或真实 ScreenCaptureKit 双路到 writer 的端到端会话。

## Task 10：最终验收（2026-08-18）

### 总结

- 自动测试、release 构建、dated app 构建、签名和 plist：**PASS**。
- 3 小时与 25 小时时间线纯计算边界：**PASS**；只计算 frame position，没有分配对应时长的静音数组。
- 受控 probe 录制 25 秒后 TERM 并恢复：**PASS**；这验证的是当前 recoverable writer/finalizer，不冒充正式 app 的真实双音源端到端录音。
- 四个会议平台、60 分钟同步、3 小时连续录音、录音中资源占用和多项人工异常场景：**UNVERIFIED**。
- 正式 app 5 分钟连续待机资源场景：**FAIL（验收流程未完成）**。只取得 242 个 1 秒样本，之后 PID `10108` 正常响应 Quit AppleEvent 并退出；Quit 发送者未知。没有向该 PID 发送 TERM/KILL，也没有自行重启。
- 整体状态：**FINAL_GATE_INCOMPLETE**。Gate 4、正式 app 范围的 Gate 5、Gate 6 和多项真实异常场景尚未满足，不能放行 release。

### TDD：长时间线边界

原 brief 给出的 RED 断言要求 `3 × 60 × 60 × 48,000` 大于 `UInt32.max`。实际数学值为 `518,400,000`，小于 `4,294,967,295`，因此原断言必然失败：

```text
XCTAssertGreaterThan failed: ("518400000") is not greater than ("4294967295")
Executed 1 test, with 1 failure
```

2026-08-18，Alan 确认修正验收规格：保留 3 小时精确值断言，另加 25 小时边界断言。25 小时为 `4,320,000,000` frames，确实超过 `UInt32.max`。未修改生产代码。

```text
env DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer \
  /usr/bin/xcrun swift test --filter LongTimelineTests

LongTimelineTests: Executed 2 tests, with 0 failures
```

### 自动测试、release 和 app bundle

| 检查 | 实际命令 | 结果 |
| --- | --- | --- |
| 全量 warnings-as-errors | `env DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer /usr/bin/xcrun swift test -Xswiftc -warnings-as-errors` | **PASS**：186 tests，0 failures，1 skipped。跳过项为只有设置 `MEETING_RECORDER_CAPTURE_UI` 才执行的视觉截图测试。 |
| Release | `env DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer /usr/bin/xcrun swift build -c release` | **PASS**：exit 0。 |
| Dated app | `./scripts/build-app.sh` | **PASS**：生成 `dist/会议录音-2026-08-18.app`，脚本内签名验证通过。没有安装或替换正式 app。 |
| 签名 | `/usr/bin/codesign --verify --deep --strict --verbose=2 dist/会议录音-2026-08-18.app` | **PASS**：valid on disk；satisfies its Designated Requirement；arm64 ad-hoc signature。 |
| Git 忽略 | `git check-ignore -v .build dist` | **PASS**：`.build/` 与 `dist/` 均由 `.gitignore` 排除。 |

构建环境：macOS 26.5.1（25F80），Xcode 26.6（17F113），Apple Swift 6.3.3，arm64。

### 隐私说明、菜单栏属性和全局快捷键

对 `dist/会议录音-2026-08-18.app/Contents/Info.plist` 的实际读取结果：

```text
CFBundleIdentifier = com.alan.local-meeting-recorder
LSUIElement = true
NSAudioCaptureUsageDescription = 用于录制会议中的系统声音。
NSMicrophoneUsageDescription = 用于录制会议中的麦克风声音。
NSScreenCaptureUsageDescription = 用于捕获系统音频，不会保存屏幕画面。
```

- 三项隐私说明、`LSUIElement` 和签名：**PASS**。
- 快捷键实现静态检查：**PASS**。使用 Carbon `RegisterEventHotKey`，源码中没有 `CGEventTap`、全局 `NSEvent` monitor、`AXIsProcessTrusted`、IOHID 或 TCC 输入监控调用；`HotkeyServiceTests` 也在全量测试中通过。
- 在本轮实际权限环境中反复按全局快捷键并观察是否出现辅助功能/输入监控提示：**UNVERIFIED**。没有用正式 app 制造录音，也没有触发或同意权限弹窗。

### 四个平台真实录音

以下四项没有实际会议，不能用已有 ScreenCaptureKit probe、通用系统声音或单元测试替代。设备与软件版本只有在真实录音时才有意义，因此均如实记为未记录。

| 平台 | 日期 | 平台版本 | 输入/输出设备 | 最终文件 | 结果与原因 |
| --- | --- | --- | --- | --- | --- |
| Zoom | 2026-08-18 | 未记录 | 未记录 | 无 | **UNVERIFIED**：没有真实 Zoom 通话、双方固定测试句、通知定位和 QuickTime 人工听取。 |
| Google Meet | 2026-08-18 | 未记录 | 未记录 | 无 | **UNVERIFIED**：没有真实 Meet 通话、双方固定测试句、通知定位和 QuickTime 人工听取。 |
| 腾讯会议 | 2026-08-18 | 未记录 | 未记录 | 无 | **UNVERIFIED**：没有真实腾讯会议通话、双方固定测试句、通知定位和 QuickTime 人工听取。 |
| 企业微信会议 | 2026-08-18 | 未记录 | 未记录 | 无 | **UNVERIFIED**：没有真实企业微信会议、双方固定测试句、通知定位和 QuickTime 人工听取。 |

因此，“系统声音和麦克风都进入正式 app 的最终 M4A”仍为 **UNVERIFIED**。已有证据只能分别证明真实 capture 能收到两路 buffer，以及合成/单元流程能写出 M4A，不能把两者拼成未执行的端到端结论。

### 60 分钟与至少 3 小时

| 场景 | 结果 | 证据缺口 |
| --- | --- | --- |
| 60 分钟双路相对同步漂移 ≤100 ms | **UNVERIFIED** | 没有执行 60 分钟录音，也没有开始/结束参考脉冲和人工持续漂移检查。 |
| 至少 3 小时不自动停止 | **UNVERIFIED** | 没有执行 3 小时真实录音。LongTimeline 单元测试只证明 frame position 可表达对应时间，不能代替连续录音。 |
| 跨午夜不拆分 | **UNVERIFIED** | 没有跨午夜录音。 |
| 3 小时文件可播放 | **UNVERIFIED** | 没有 3 小时文件。 |
| 文件比例 <70 MB/小时 | **UNVERIFIED** | 没有 3 小时实测。25 秒恢复 probe 的输出为 241,769 bytes / 20.969333 秒，折算 41.507 MB/小时，只是短时合成正弦波旁证，不能作为 3 小时通过证据。 |

### 异常和重复操作

| 场景 | 结果 | 实际证据或原因 |
| --- | --- | --- |
| 断开/切换麦克风，系统声音继续且警告 | **UNVERIFIED** | 未操作用户硬件；自动测试仅验证事件处理，不代替真实设备切换。 |
| 拒绝权限后无假录音 | **UNVERIFIED** | 没有撤销权限或点击拒绝，也没有触发权限弹窗。权限单元测试通过，只算代码级旁证。 |
| 注入低于 1 GB 容量并拒绝开始 | **UNVERIFIED** | 没有构造真实低容量卷。`RecordingStoreTests.testPrepareRejectsWhenAvailableCapacityIsBelowOneGigabyte` 通过，只算注入式单元证据。 |
| 录制 25 秒后强制结束并恢复 | **PASS（受控 probe）** | 独立临时根目录 `/tmp/meeting-recorder-task10-recovery-25s.KAUCQ9`；只向本轮创建并核对 executable 的 PID `51179` 发送 TERM，wait status `143`；正式 app PID 未被操作。 |
| 恢复失败时保留 working file | **PASS（自动测试）/ 真实失败场景 UNVERIFIED** | 全量测试中的 `testFailedRecoveryLeavesWorkingFileAndDoesNotPublishPartialOutput` 通过；25 秒 probe 是成功恢复，未制造真实恢复失败，不能替代该场景。 |
| 快速按快捷键 20 次，最多一个活动会话和一个最终文件 | **UNVERIFIED** | 没有对正式 app 连按快捷键，以免录制用户环境或写入默认录音目录；并发/重复 action 自动测试不能代替真实 20 次操作。 |

25 秒恢复 probe 的实际结果：

```text
ready pid=51179 signal=TERM strategy=fragmented
sent signal=TERM to exact PID 51179 after 25 seconds
waitStatus=143
durationSeconds=20.969333
audioTracks=1
aacTrack=true
playable=true
peakAmplitude=0.258452
result=PASS
afinfo: 2 ch, 48000 Hz, aac, estimated duration 20.969333 sec
working file after recovery: absent
recovered file: /tmp/meeting-recorder-task10-recovery-25s.KAUCQ9/task10-25s-未完整恢复.m4a
```

这是独立 probe 的可恢复 writer/finalizer 实测，不是 Zoom 等平台录音，也不是 ScreenCaptureKit 双路到正式 app 最终文件的端到端实测。未模拟断电、内核崩溃或磁盘硬件损坏。

### 资源占用

正式 app 只读待机采样开始前，PID `10108` 的 executable 已核对为 `/Users/alan/Applications/会议录音.app/Contents/MacOS/MeetingRecorderApp`。计划每秒采一次，共 300 秒；实际只取得 242 个样本，之后 PID 不再存在。

```text
samples=242
averageCPU=0.206%
peakCPU=7.6%
averageRSS=11.62MB
peakRSS=32.41MB
```

- 已采到的 242 秒平均 CPU 接近 0%，RSS 远低于 150 MB，但未达到完整待机 5 分钟，所以场景判定为 **FAIL（验收流程未完成）**，不能写 PASS。
- PID `10108` 在受控 25 秒恢复 probe 完成时仍被确认存活，随后在只读采样结束前退出。Controller 日志于 2026-08-18 10:03:53.654 记录 `Handling Quit AppleEvent`，之后依次记录 `applicationShouldTerminate: NSTerminateNow`、`reply...YES` 和 `Termination complete`；runningboard/launchd 将其记为 voluntary，termination 为 `(0,0,0)`，且没有 crash report。现有证据说明这是正常 Quit，不支持“app crash”或“系统回收”的判断；Quit 发送者仍为 **UNVERIFIED**。本轮没有向正式 app 发送 TERM/KILL、没有操作菜单或快捷键、没有重新安装或重启。
- 录音 10 分钟平均 CPU <5%、内存 <150 MB：**UNVERIFIED**。为避免捕获用户声音、写入默认录音目录或操作正式 app，本轮没有启动正式录音；probe 资源不能代替正式 app。

### Final Completion Gate

| Gate | 状态 |
| --- | --- |
| 1. `swift test` 与 release build | **PASS** |
| 2. 三项隐私说明、`LSUIElement` 和签名 | **PASS** |
| 3. 全局快捷键无需辅助功能/输入监控 | **PASS（实现与自动测试）/ 实际权限环境 UNVERIFIED** |
| 4. 系统声音和麦克风进入正式 app 最终 M4A | **UNVERIFIED** |
| 5. 强制结束恢复且失败不删除工作文件 | **TERM 后恢复 PASS（受控 probe）；失败保留 working PASS（自动测试）/ 真实失败与正式 app 端到端 UNVERIFIED** |
| 6. 60 分钟、3 小时、大小、资源实测 | **FAIL（待机 5 分钟未完成）；60 分钟、3 小时、大小与录音资源 UNVERIFIED** |
| 7. 四个平台逐项标记 | **PASS（均已诚实标为 UNVERIFIED）** |
| 8. 草稿、缓存、录音和本地 app 不入 Git | **PASS**：媒体只在 `/tmp`；`.build/`、`dist/` 均被忽略。 |
| 9. 最终差异只含 Task 10 文件，独立提交 | 提交前检查并记录在 task report。 |
